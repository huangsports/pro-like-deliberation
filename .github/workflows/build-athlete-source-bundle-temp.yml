name: Temporary source bundle runner test

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner
        run: |
          echo "runner-ok"
          uname -a
